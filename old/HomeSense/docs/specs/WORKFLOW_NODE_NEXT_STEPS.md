# Workflow Node Next Steps

## Immediate next step

1. Add `WorkflowV0`, `WorkflowNodeV0`, and `WorkflowEdgeV0` types into the backend shared model area.
2. Create a small local example workflow registry.
3. Add a capability-based node picker input source for future frontend use.
4. Do not build a full drag-and-drop UI yet.

## First recommended workflow examples
- Go home
- Navigate back
- Open TV via voice hub
- Find text and click
- Observe UI tree then fallback to screenshot

## Important rule

Visual orchestration must target capabilities, not raw tool actions.
