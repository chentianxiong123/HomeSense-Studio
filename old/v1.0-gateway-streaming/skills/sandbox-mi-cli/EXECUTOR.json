{
  "name": "sandbox-mi-cli",
  "executable": "./runner.mjs",
  "protocol": "in_process_module",
  "cwd": ".",
  "args": [],
  "timeout_ms": 30000,
  "actions": {
    "scene_execute": {
      "description": "Execute a Mijia scene inside the sandbox home.",
      "params_schema": {
        "scene_id": "string?",
        "scene_name": "string?",
        "home_id": "string?"
      }
    },
    "speaker_execute": {
      "description": "Send a XiaoAi-style text command inside the sandbox home.",
      "params_schema": {
        "text": "string",
        "silent": "boolean?",
        "did": "string?"
      }
    },
    "speaker_play": {
      "description": "Play speech or media text inside the sandbox home.",
      "params_schema": {
        "text": "string",
        "did": "string?"
      }
    },
    "device_action": {
      "description": "Apply a MI device action inside the sandbox home.",
      "params_schema": {
        "did": "string",
        "capability": "string",
        "params": "array?"
      }
    },
    "device_ir_press": {
      "description": "Press a cached IR key inside the sandbox home.",
      "params_schema": {
        "did": "string",
        "key_id": "string"
      }
    }
  }
}
