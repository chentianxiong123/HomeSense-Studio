[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "adb-cli"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[project.scripts]
adb-cli = "adb_cli.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/adb_cli"]