# 2026-05-26 · chat 模块换血完成

## 背景
废弃老的 chat + conversation 模块，chat2 上位，接真实 LLM SSE 流式对话。

## 变更清单

### 删除
- `modules/chat/` — 老 chat 模块
- `modules/conversation/` — 老 conversation 模块（含 HistoryItem 类型）
- `demo-standalone-chat.ts` — 独立 demo

### 重命名
- `modules/chat2/` → `modules/chat/` — chat2 晋升为主力
- 路由 `/api/chat2/*` → `/api/chat/*`

### app.ts
- 清理所有老 chat/conversation 的 import
- 注册新 chat 模块路由

### 后端 repository.ts
- 新增 `conversations` 表
- 新增 `createConversation(title?)` 方法
- 新增 `addMessage(convId, role, content)` 方法
- service.ts 暴露对应新方法

### 后端 routes.ts
- `POST /api/chat/stream` 接入 `llmService.chatStream({ slot: 'planner' })`
- 实现真实 LLM SSE 流式响应

### agent-runtime
- `HistoryItem` 类型内联定义于 agent-runtime 内（此前依赖已删除的 conversation 模块）

### 前端 ChatView.vue
- 删除 mock SSE 逻辑
- 接入真实 `/api/chat/stream` 端点
- 带 `currentConversationId` 状态管理

### 类型检查
- `tsc --noEmit` 后端 ✅ 零新增错误
- `vue-tsc --noEmit` 前端 ✅ 零新增错误

## 下一步
1. 配 LLM provider API key（planner slot）
2. 前端 `VITE_API_BASE` 指向后端
3. 打开聊天页发送消息，验证 SSE 流式回复
4. 前端历史消息懒加载（触顶游标分页）