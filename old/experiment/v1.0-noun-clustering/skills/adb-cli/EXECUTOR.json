{
  "name": "sandbox-adb-cli",
  "executable": "./runner.mjs",
  "protocol": "in_process_module",
  "cwd": ".",
  "args": [],
  "timeout_ms": 30000,
  "actions": {
    "wait": {
      "description": "Wait in the virtual Android TV runtime.",
      "params_schema": {
        "seconds": "number"
      }
    },
    "ensure_connected": {
      "description": "Ensure the virtual Android TV runtime is connected.",
      "params_schema": {
        "initial_wait_seconds": "number?",
        "max_attempts": "number?",
        "backoff_seconds": "number?"
      }
    },
    "list_packages": {
      "description": "List installed packages, optionally filtered by keyword.",
      "params_schema": {
        "keyword": "string?"
      }
    },
    "launch_app": {
      "description": "Launch a package in the virtual Android TV runtime.",
      "params_schema": {
        "package": "string"
      }
    },
    "back": {
      "description": "Press Back in the sandbox runtime.",
      "params_schema": {}
    },
    "home": {
      "description": "Press Home in the sandbox runtime.",
      "params_schema": {}
    },
    "enter": {
      "description": "Press Enter/OK in the sandbox runtime.",
      "params_schema": {}
    },
    "volume_up": {
      "description": "Increase volume in the sandbox runtime.",
      "params_schema": {}
    },
    "volume_down": {
      "description": "Decrease volume in the sandbox runtime.",
      "params_schema": {}
    },
    "power": {
      "description": "Toggle power in the sandbox runtime.",
      "params_schema": {}
    },
    "wake": {
      "description": "Wake the sandbox runtime.",
      "params_schema": {}
    },
    "tap": {
      "description": "Tap coordinates in the sandbox runtime.",
      "params_schema": {
        "x": "number",
        "y": "number"
      }
    },
    "input_text": {
      "description": "Input text in the sandbox runtime.",
      "params_schema": {
        "text": "string"
      }
    },
    "screenshot": {
      "description": "Return a synthetic screenshot result from the sandbox runtime.",
      "params_schema": {}
    },
    "current_app": {
      "description": "Read the current app from the sandbox runtime.",
      "params_schema": {}
    },
    "ui_tree": {
      "description": "Return a synthetic UI tree from the sandbox runtime.",
      "params_schema": {}
    },
    "tap_element": {
      "description": "Tap an element by index or text in the sandbox runtime.",
      "params_schema": {
        "index": "number?",
        "text": "string?"
      }
    },
    "swipe": {
      "description": "Swipe in the sandbox runtime.",
      "params_schema": {
        "start_x": "number",
        "start_y": "number",
        "end_x": "number",
        "end_y": "number",
        "duration": "number?"
      }
    }
  }
}
