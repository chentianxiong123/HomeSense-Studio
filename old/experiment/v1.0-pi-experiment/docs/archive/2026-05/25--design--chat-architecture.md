# 2026-05-25 · Chat 模块架构设计决策

> ✅ 完成 | 架构设计

## 探索成果
- 确定独立 chat.db（不与 homesense.db 共用）
- 确定纯聊天模式，不走 agent runtime
- 确定普通滚动，不用虚拟滚动

## 技术栈
- SQLite WAL 模式（独立 chat.db）
- 游标分页（基于 message id）
- 纯 LLM 对话（无工具调用/agent 编排）

## 关键决策
- 独立 chat.db → 聊天数据与业务数据隔离，不互相污染
- 纯聊天不走 agent runtime → 简单可靠，不引入工具调用复杂度
- 普通滚动不用虚拟滚动 → 移动端兼容，实现简单

文件: 无（架构设计阶段）